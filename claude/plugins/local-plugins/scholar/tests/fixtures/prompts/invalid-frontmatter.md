---
prompt_version: "2.0"
prompt_type: "lecture-notes"
  invalid_indent: this is wrong
  variables:
    required: [topic
---

# This file has malformed YAML frontmatter

The YAML is not valid due to indentation and syntax errors.
